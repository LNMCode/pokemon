<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="Resource"
            type="com.longnmp.pokemon.R" />

        <variable
            name="viewModel"
            type="com.longnmp.pokemon.ui.home.HomeViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.home.HomeFragment">

        <TextView
            android:id="@+id/textView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Hello"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:foregroundGravity="center"
            app:adapterRVPokemon="@{viewModel.pokemonData}"
            app:gridLayoutSizeSpacing="@{@dimen/width_card_view_item_home}"
            app:gridLayoutSpaceBetween="@{true}"
            app:gridLayoutSpanCount="@{@integer/number_columns_layout_home}"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/textView"
            app:spanCount="@integer/number_columns_layout_home"
            tools:listitem="@layout/layout_item_home" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>